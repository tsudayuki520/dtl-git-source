<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlust.sportbackend.Mapper.EventMapper">

    <select id="selectBySportsMeetingId" resultType="Event">
        SELECT e.*, g.name AS group_type_name FROM event e
        LEFT JOIN group_type g ON e.group_type_id = g.id
        WHERE e.sports_meeting_id = #{sportsMeetingId} AND e.is_deleted = 0
        ORDER BY e.category, e.group_type_id, e.name
    </select>

    <select id="selectById" resultType="Event">
        SELECT e.*, g.name AS group_type_name FROM event e
        LEFT JOIN group_type g ON e.group_type_id = g.id
        WHERE e.id = #{id} AND e.is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO event (sports_meeting_id, name, category, gender, group_type_id, allow_register, register_limit, status)
        VALUES (#{sportsMeetingId}, #{name}, #{category}, #{gender}, #{groupTypeId}, #{allowRegister}, #{registerLimit}, #{status})
    </insert>

    <update id="updateById">
        UPDATE event
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="category != null">category = #{category},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="groupTypeId != null">group_type_id = #{groupTypeId},</if>
            <if test="allowRegister != null">allow_register = #{allowRegister},</if>
            <if test="registerLimit != null">register_limit = #{registerLimit},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="softDeleteById">
        UPDATE event SET is_deleted = 1 WHERE id = #{id}
    </update>

</mapper>
