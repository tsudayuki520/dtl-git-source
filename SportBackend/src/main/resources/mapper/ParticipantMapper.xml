<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlust.sportbackend.Mapper.ParticipantMapper">

    <select id="selectById" resultType="Participant">
        SELECT * FROM participant WHERE id = #{id}
    </select>

    <select id="selectByUserCodeAndSportsMeetingId" resultType="Participant">
        SELECT * FROM participant
        WHERE user_code = #{userCode} AND sports_meeting_id = #{sportsMeetingId}
    </select>

    <select id="selectBySportsMeetingId" resultType="Participant">
        SELECT * FROM participant
        WHERE sports_meeting_id = #{sportsMeetingId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByTeamId" resultType="Participant">
        SELECT * FROM participant
        WHERE team_id = #{teamId}
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO participant (sports_meeting_id, team_id, user_code, name, phone, gender, college, major)
        VALUES (#{sportsMeetingId}, #{teamId}, #{userCode}, #{name}, #{phone}, #{gender}, #{college}, #{major})
    </insert>

    <update id="updateById">
        UPDATE participant
        <set>
            <if test="teamId != null">team_id = #{teamId},</if>
            <if test="userCode != null">user_code = #{userCode},</if>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="college != null">college = #{college},</if>
            <if test="major != null">major = #{major},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM participant WHERE id = #{id}
    </delete>

</mapper>
