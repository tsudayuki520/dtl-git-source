<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlust.sportbackend.Mapper.ParticipantMapper">

    <select id="selectById" resultType="Participant">
        SELECT * FROM participant WHERE id = #{id}
    </select>

    <select id="selectByUserCodeAndSportsMeetingId" resultType="Participant">
        SELECT * FROM participant
        WHERE user_code = #{userCode} AND sports_meeting_id = #{sportsMeetingId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO participant (sports_meeting_id, user_code, name, phone, gender, college, major)
        VALUES (#{sportsMeetingId}, #{userCode}, #{name}, #{phone}, #{gender}, #{college}, #{major})
    </insert>

</mapper>
