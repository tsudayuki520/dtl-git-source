<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlust.sportbackend.Mapper.RecordMapper">

    <select id="selectAll" resultType="Record">
        SELECT * FROM record
        ORDER BY record_time DESC, create_time DESC
    </select>

    <select id="selectById" resultType="Record">
        SELECT * FROM record WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO record (group_type, event_name, unit, name, score, record_time, venue)
        VALUES (#{groupType}, #{eventName}, #{unit}, #{name}, #{score}, #{recordTime}, #{venue})
    </insert>

    <update id="updateById">
        UPDATE record
        <set>
            <if test="groupType != null">group_type = #{groupType},</if>
            <if test="eventName != null">event_name = #{eventName},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="name != null">name = #{name},</if>
            <if test="score != null">score = #{score},</if>
            <if test="recordTime != null">record_time = #{recordTime},</if>
            <if test="venue != null">venue = #{venue},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM record WHERE id = #{id}
    </delete>

</mapper>
