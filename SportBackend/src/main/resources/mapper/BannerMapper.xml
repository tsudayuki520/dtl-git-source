<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlust.sportbackend.Mapper.BannerMapper">

    <select id="selectActiveBanners" resultType="com.dlust.sportbackend.entity.Banner">
        SELECT id, image_url, title, sort_order, status, create_time, update_time
        FROM banner
        WHERE status = 1
        ORDER BY sort_order ASC
    </select>

    <insert id="insertBanner" parameterType="com.dlust.sportbackend.entity.Banner" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO banner (image_url, title, sort_order, status)
        VALUES (#{imageUrl}, #{title}, #{sortOrder}, #{status})
    </insert>

</mapper>
