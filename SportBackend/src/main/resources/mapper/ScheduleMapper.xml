<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlust.sportbackend.Mapper.ScheduleMapper">

    <select id="selectBySportsMeetingId" resultType="Schedule">
        SELECT * FROM schedule
        WHERE sports_meeting_id = #{sportsMeetingId} AND is_deleted = 0
        ORDER BY create_time ASC
    </select>

    <select id="selectById" resultType="Schedule">
        SELECT * FROM schedule WHERE id = #{id} AND is_deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO schedule (sports_meeting_id, name, status)
        VALUES (#{sportsMeetingId}, #{name}, #{status})
    </insert>

    <update id="updateById">
        UPDATE schedule
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="softDeleteById">
        UPDATE schedule SET is_deleted = 1 WHERE id = #{id}
    </update>

</mapper>
